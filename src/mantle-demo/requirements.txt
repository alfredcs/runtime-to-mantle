# Core demo server — enough to run in DEMO (mock) mode with zero AWS setup.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27

# OPTIONAL — only needed to hit the real Bedrock Mantle endpoint in LIVE mode.
# Uncomment and `pip install` these, then set MANTLE_LIVE=1 plus AWS creds.
# boto3>=1.34                      # SigV4 fallback auth
# aws-bedrock-token-generator>=1.0 # short-term Bearer token generation
